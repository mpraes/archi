# Maintainer: mpraes <mpraes@example.com>
# Generated by CI; do not edit by hand.
# The sha256 and pkgver are overwritten at release time by the
# KSXGitHub/github-actions-deploy-aur action's `commit_username`/etc. inputs,
# but this template sets sane defaults for local builds.

pkgname=archi-bin
pkgver=0.1.0
pkgrel=1
pkgdesc="Static-analysis CLI that computes architectural metrics and serves a local web UI"
arch=('x86_64' 'aarch64')
url="https://github.com/mpraes/archi"
license=('MIT')
provides=('archi')
conflicts=('archi' 'archi-git')
options=('!strip' '!emptydirs')

source_x86_64=("archi-${pkgver}-linux-amd64.tar.gz::${url}/releases/download/v${pkgver}/archi-v${pkgver}-linux-amd64.tar.gz")
sha256sums_x86_64=('PLACEHOLDER')

source_aarch64=("archi-${pkgver}-linux-arm64.tar.gz::${url}/releases/download/v${pkgver}/archi-v${pkgver}-linux-arm64.tar.gz")
sha256sums_aarch64=('PLACEHOLDER')

package() {
    install -Dm755 "${srcdir}/archi-v${pkgver}-linux-${CARCH}/archi" "${pkgdir}/usr/bin/archi"
}